; ; ; ; protected class uses extends @ requires ( ) long @ exports . exports . fEl . uses [ ] [ ] [ ] implements uses < ? > , short , module < ? , ? > . uses < ? , ? > . open . sealed @ transitive . P2 . requires . with [ ] open . @ module ( { } ) @ to @ var @ var [ ] { { } static { } synchronized private sealed ( ) { } ; } final @ interface var { native volatile synchronized volatile enum uses { , ; ; ; ; ; ; } ; ; native transient class yield extends int [ ] [ ] { } ; class sealed { ; } ; }