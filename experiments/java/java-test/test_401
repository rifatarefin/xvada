transitive . @ to ( this != this ) package uses ; strictfp interface yield < @ c0 uses extends sealed & opens . provides . requires [ ] , @ module ( ) opens extends @ permits open . permits . uses [ ] > extends permits . @ var ( { , } ) @ permits . sealed . var ( { , } ) float { }