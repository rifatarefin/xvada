@ module @ with ( this > super ) package uses . permits ; ; strictfp strictfp sealed enum open implements @ transitive @ requires ( ) @ exports @ requires record . record < ? , ? , ? > . opens { uses ( ) { static { } } , with ( ) , @ module . module . yield @ requires sealed . @ exports @ var permits ( ) { } , exports ; ; static { ; } transient native synchronized @ interface yield { ; } ; }