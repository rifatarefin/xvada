import static uses . * ; public interface opens < x26 . @ open @ module requires extends boolean & to . module . var . transitive [ ] , transitive extends @ uses . permits . var ( { , } ) @ record . with . provides . requires . opens float > extends yield < ? , ? , ? , ? > . exports < ? > @ requires . with ( ) [ ] [ ] { } ;