; import opens . * ; ; public private @ open ( ) public @ record ( ) non-sealed @ interface var { @ interface open { } } ; requires . uses . @ transitive protected @ permits . module . with . module . var record module ( ) { } ;