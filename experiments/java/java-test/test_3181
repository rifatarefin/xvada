package transitive . S . requires ; import static H6u$ . with . sealed ; ; public static interface opens < permits , @ q ( { } ) @ record ( { } ) transitive extends opens . open . requires . to . opens [ ] [ ] [ ] [ ] [ ] & to & open . requires . permits . opens > permits char { class var { } } protected class opens extends boolean @ provides @ opens ( ) [ ] [ ] [ ] implements permits . record . permits . @ uses ( ) @ yield ( { } ) @ exports . exports ( { , } ) uses . permits . yield . @ exports ( ) boolean , @ exports @ transitive ( ) boolean permits @ permits ( ) short , opens < ? > { static { } ; class sealed extends boolean [ ] [ ] { } }