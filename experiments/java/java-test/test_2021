record var ( long record , yield . permits [ ] [ ] [ ] opens , byte [ ] BS0 , provides sealed , provides to ) implements opens < ? > { { } abstract transient var { { } return ; } static { continue ; return ; } }