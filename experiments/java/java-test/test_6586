import static sealed ; import static record . record . to . var . with ; interface opens extends provides < ? > , @ yield . exports . opens . with . to @ with ( ) @ record requires , to < ? > [ ] permits with . exports . record . opens . @ module ( { } ) float [ ] { synchronized class to { ; } ; record opens ( ) { ; ; ; } } private non-sealed enum exports { uses , ; } private protected interface uses < @ provides . permits . provides ( ) open . @ uses ( ) module > permits record , float , char [ ] , transitive < ? , ? > . b [ ] @ to . permits . record . record ( ) module . record . exports . @ uses [ ] { }