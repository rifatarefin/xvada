record permits < @ yield ( { , } ) to , to extends long & long [ ] [ ] [ ] [ ] , @ var . transitive . with . to ( { } ) @ yield ( { , } ) @ var . requires . permits ( { , } ) b$5 , record , exports extends @ opens ( { , } ) uses , @ uses open extends long > ( short [ ] provides ) implements @ record ( ) exports . @ module @ uses permits < ? , ? > { final opens { requires : break ; continue uses ; ; } ; }