permits . @ module ( record = { { , } , { , } , } , uses = { { , } , } ) package yield ; public non-sealed enum with implements short , @ sealed ( { } ) permits . J6 < ? , ? , ? , ? , ? , ? > , @ yield . with var . provides . @ permits @ permits ( ) short @ yield ( ) @ f [ ] @ module . var . permits ( ) [ ] , @ exports ( { } ) opens . C . C4 . to . @ uses ( { } ) @ yield ( { } ) @ sealed with . @ module ( { , } ) @ provides . provides ( ) uses < ? > . requires . sealed @ F9 . to [ ] [ ] { , }