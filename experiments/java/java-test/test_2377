package open . record ; import static uses . sealed ; ; static strictfp public interface transitive < @ with ( ) @ record ( ) @ opens exports extends short , @ transitive ( { } ) exports extends record . provides , @ provides r extends @ transitive ( { , } ) module . uses . permits . record . permits & byte > permits yield . yield < ? , ? > . to . with . with @ open @ sealed ( { , } ) [ ] @ open . sealed . provides . permits @ uses ( ) [ ] { native record sealed ( ) { } ; } ; @ interface record { }