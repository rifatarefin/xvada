package to . yield ; ; interface provides < @ L . sealed . provides . with . to . with . exports permits . transitive . a8 . @ to requires > extends @ opens . yield . sealed . requires ( { , } ) @ with @ requires . exports . o @ with . permits @ with boolean @ to [ ] with . @ uses ( { , } ) [ ] @ with [ ] , record . @ module ( { } ) @ uses @ uses . u . provides . transitive ( ) provides , open . @ uses ( { } ) Z . g2 . @ with @ exports . with . record ( { , } ) permits . @ module ( { , } ) @ requires char @ yield . requires . open . record [ ] { volatile transient void with ( ) ; }