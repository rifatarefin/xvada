import static uses . open ; ; import static provides . * ; import permits ; record e ( ) { } private interface requires extends @ var . open ( ) permits < ? , ? > , @ exports . z7 . record @ opens ( { , } ) byte @ transitive . exports [ ] , transitive . @ permits ( ) module . to < ? > , double @ to ( ) transitive . @ requires [ ] @ record . provides . yield [ ] [ ] , @ with char , @ yield . requires boolean , var . @ to byte [ ] , @ with . var @ uses ( { , } ) @ with boolean , requires . transitive . permits . record . transitive . @ exports @ record ( { , } ) opens . @ to ( { } ) permits permits permits . module < ? , ? > . permits @ record . exports @ module ( ) [ ] , @ uses with . permits < ? > , var . record { } ;