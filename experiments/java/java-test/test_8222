@ record package opens . open . permits . uses ; import static requires . sealed . record . * ; import opens . * ; interface open < @ transitive ( { , } ) provides . @ to ( ) permits > permits @ sealed ( { } ) @ provides ( { , } ) uses < ? , ? , ? > . opens < ? , ? > . provides < ? , ? , ? , ? , ? > @ sealed ( ) @ module . var provides . @ provides ( ) @ record ( { } ) @ module ( ) [ ] @ opens ( { , } ) transitive . sealed . yield . @ opens ( { } ) [ ] , float , int { ; } ; ;