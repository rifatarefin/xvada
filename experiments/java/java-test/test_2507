@ requires ( sealed = super , open = @ module , provides = super , with = super ) package permits . var ; ; import $c . * ; ; import opens . permits . permits . * ; import static sealed . permits . requires . var . to . yield . permits . * ; ; import opens . opens . uses . * ; import static open ; import sealed . sealed . Fd . uses . record . * ; import static open ; import U . * ; ;