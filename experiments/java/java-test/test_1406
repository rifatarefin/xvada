@ exports . to . var @ to ( super :: < ? > uses ) @ requires @ var . provides to . @ permits ( module = this , o = this , permits = this , permits = super ) package with . to . transitive ; public static final @ interface sealed { volatile interface with { ; } ; } sealed abstract static private strictfp interface record < @ record ( ) provides , exports extends boolean & short > extends with < ? > . requires { ; native synchronized record yield ( ) { } }