@ provides ( uses = { { , } , { , } } , uses = super , requires = permits . @ provides ( { , } ) , sealed = @ with . var . var . transitive , uses = this , record = { , } ) n4M . @ opens ( ) @ permits . var . record . requires . yield . open ( provides = super , uses = @ uses ( ) ) package opens . open ; ; import requires . o0 . * ;