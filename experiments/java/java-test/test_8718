import B ; final enum to implements sealed . uses < ? , ? > . G . with { @ opens . sealed ( { , } ) @ record . open . with . sealed @ sealed with ( ) ; transient volatile enum var { , ; ; } } record provides ( short record , char to , to . to . transitive . open opens , opens provides ) { } static class l implements @ requires @ exports short permits @ to int { transient to ( ) { } ; static { } }