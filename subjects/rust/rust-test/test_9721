const _ : impl ( $crate :: ) + crate < > + = return .. [ ] ..= .. ( ) ; macro_rules ! macro_rules [ [ $ ( ( ) ) * $ ( $ ( [ ] ) ? $ ( ( ) ) + { } ) ; * >>= macro_rules $crate "," ^ macro_rules { } ] => { { } } ; ] ;