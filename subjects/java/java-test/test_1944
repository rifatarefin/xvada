@ PB . to @ provides ( transitive . @ uses ( { , } ) ) @ yield . permits . w . open . transitive ( exports = { , } , provides = @ yield . transitive . requires . requires . var ( ) , requires = { , } , module = super , exports = { { , } , } ) package yield . transitive . with . provides . uses . sealed ;