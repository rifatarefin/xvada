@ uses . with . permits ( ) @ exports . provides @ sealed . uses . uses @ opens ( ) @ M @ var package provides ; sealed class yield implements @ requires . with . yield . sealed . open . opens . with . with . transitive ( ) @ with float , sealed < ? , ? > . sealed exports . @ record ( ) [ ] [ ] { @ interface module { } }