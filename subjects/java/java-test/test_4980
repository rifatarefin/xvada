import static M . * ; import static var . * ; import module . requires . open ; ; ; import static open . * ; enum transitive implements @ opens with . sealed . @ permits var . to < ? > . open . permits < ? > { record . with . @ permits with ( ) { } , @ permits ( { } ) @ exports @ permits @ uses . yield @ module . sealed ( { , } ) @ transitive . to . exports . record . open . requires ( ) yield ( ) { } , ; }