package uses ; import with . record ; protected public public enum with implements boolean [ ] , @ record ( ) char { @ module uses , @ to ( { , } ) open , @ with . requires . opens . module @ module yield , module . with . @ record provides , provides ( ) , ; }