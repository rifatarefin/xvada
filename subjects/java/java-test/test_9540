to . permits . @ with @ provides . opens . permits package uses ; import static permits . uses . SY3o4 ; interface sealed < @ module @ sealed . provides @ sealed @ yield . permits ( { , } ) @ uses to extends yield . @ open ( { , } ) @ sealed @ permits @ uses module . transitive . uses . X81 . @ requires ( { , } ) requires . opens . sealed . @ exports @ transitive ( { } ) @ provides @ record float > extends W { }