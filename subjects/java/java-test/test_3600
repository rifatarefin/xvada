@ C @ exports . requires . y . Eh ( { this , } ) @ requires ( ) @ transitive . module . with ( ) @ transitive package opens ; import static Z . * ; import var . sealed . * ; ; ; ; ; @ interface n { } private static final class record < yield extends @ yield @ yield . permits ( ) long [ ] [ ] & short [ ] [ ] [ ] > implements @ with . open module < ? , ? > , @ record ( ) exports < ? > , @ X6 int , @ uses requires . open . @ permits to . @ sealed ( { , } ) @ to ( ) double opens . module . @ exports ( ) [ ] @ transitive . var @ open @ opens ( ) [ ] permits @ var ( { , } ) int @ opens sealed . opens . with . requires . @ exports yield . @ permits ( ) @ transitive [ ] , int , record . record < ? > [ ] { }