import static module . opens . with . * ; non-sealed non-sealed interface open < with . @ var to extends @ exports ( ) opens [ ] & exports . exports [ ] [ ] & requires , requires . open . @ to @ requires @ j opens , @ transitive ( { , } ) @ module ( { , } ) @ var . provides ( { } ) @ permits . provides . record @ module @ permits exports . open . with . @ to exports , @ var . exports . to ( { } ) permits extends @ transitive ( ) with . transitive . sealed [ ] [ ] , @ uses ( ) @ permits ( { } ) var extends short [ ] [ ] > extends @ exports ( ) sealed < ? , ? > . exports . uses < ? > . permits [ ] { ; void var ( ) [ ] [ ] [ ] [ ] ; } sealed enum var { uses , record . @ var permits { } , opens . @ provides ( { , } ) transitive ( ) { static { } ; ; static { } ; ; static { } } ; transient non-sealed transient volatile synchronized interface exports { } }