permits . permits . requires . with . @ module @ to . provides @ requires ( ) requires . @ open package module ; ; ; non-sealed strictfp class open < @ uses sealed > { class sealed extends double [ ] [ ] [ ] { ; } } ; ;