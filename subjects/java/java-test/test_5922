import open ; ; strictfp protected final class transitive < yield extends @ module ( { } ) boolean & short > extends long implements byte permits @ requires . uses . to ( ) requires < ? , ? , ? , ? > , @ provides @ transitive ( ) double [ ] , requires @ record . record @ module ( { , } ) [ ] [ ] @ exports ( { } ) @ module [ ] @ permits [ ] @ transitive ( { , } ) [ ] [ ] [ ] @ with . transitive . requires . record . provides [ ] [ ] { }