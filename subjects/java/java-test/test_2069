import static opens . open . u . * ; import open . * ; enum permits implements transitive . s . opens . transitive . module . @ uses @ sealed . open @ open exports < ? > { , ; static { ; ; ; break transitive ; continue sealed ; } native transient transient @ interface opens { ; } }