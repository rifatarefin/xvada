class yield < with , sealed . transitive . @ with ( { , } ) @ var ( { } ) module > implements @ with ( ) @ requires uses < ? , ? , ? , ? , ? > , @ with float , @ permits . module . requires . opens . record . permits . var ( { , } ) @ record int , opens < ? , ? , ? > . open [ ] [ ] [ ] permits float , @ requires exports . with uses . sealed . @ to [ ] { } interface open permits @ var . sealed . record ( ) var . open . @ to ( ) short , record < ? , ? , ? , ? > . record < ? > { ; } record provides ( ) implements double [ ] { ; }