@ transitive ( super || super ) K5y3 . @ requires ( sealed = super ) package requires ; enum transitive implements uses . @ record ( ) @ sealed @ Z ( { } ) char { @ record module ( ) { } , } public static protected public static class permits < transitive , @ module @ open ( ) uses , @ with ( ) record > permits int { static { return ; opens : break ; } } final record permits ( to . permits [ ] [ ] [ ] f ) { synchronized opens { break ; ; } }