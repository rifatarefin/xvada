package yield ; ; abstract private record uses ( ) implements char , @ transitive ( { } ) uses , short { sealed { for ( ; ; ) break ; } } ; static @ interface with { native enum sealed { , ; ; } int record ( ) ; ; @ interface provides { } ; }