requires . @ permits ( requires . @ exports ) package with ; import static provides . * ; non-sealed interface record < @ transitive . opens . module ( ) permits extends byte , exports > extends @ yield @ record ( { , } ) @ m . with ( { , } ) long , @ requires @ permits . uses opens < ? > [ ] module . sealed . @ var @ requires . provides . yield . B296 @ exports [ ] [ ] @ open [ ] [ ] @ requires [ ] I87 . open . to . sealed . provides . @ provides ( { , } ) @ open ( ) [ ] @ module requires . @ exports [ ] , @ sealed . record ( ) uses < ? > . requires < ? > . yield < ? , ? , ? > . provides < ? , ? , ? > . with < ? > . yield < ? > . transitive < ? , ? > . exports < ? , ? > , requires < ? , ? , ? > @ sealed ( { , } ) [ ] @ yield [ ] @ opens [ ] , @ sealed int @ open ( { } ) @ permits . requires . with ( { , } ) @ opens ( ) @ sealed . permits ( { } ) [ ] [ ] , int @ opens ( { , } ) with . with . @ yield [ ] yield . @ permits ( ) [ ] , provides { ; }